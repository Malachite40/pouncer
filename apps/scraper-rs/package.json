{
    "name": "@pounce/scraper-rs",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "dev": "cargo run",
        "build": "cargo build --release",
        "start": "cargo run --release",
        "test": "cargo test",
        "lint": "cargo fmt --check"
    }
}
